# python -m point2pose.model_tracking.reconstruct --config configs/reconstruct/export_test.yaml
# Any of these can still be overridden on the command line, e.g.:
#   python -m point2pose.model_tracking.reconstruct --config configs/reconstruct/export_test.yaml --max_steps 2000
results_path: debug/export_test
n_views: 100
pose_warmup_steps: 1000
max_steps: 5000
model: 2dgs
bg_alpha_lambda: 0.5
extract_mesh: true
init_opacity: 0.1
pose_jump_max_trans: 0.05
view_coverage_thresh: 0.5
sh_degree: 0
ssim_lambda: 0.0
poisson_depth: 6
